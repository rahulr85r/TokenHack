{
  "blinkit": {
    "When someone signs in, what tokens do we hand back and how do they end up in the browser?": "accessToken refreshToken generatedAccessToken generatedRefreshToken jsonwebtoken jwt sign res.cookie httpOnly cookieOption localStorage loginController userSlice",
    "When an admin uploads a product image, where does the file physically end up?": "multer memoryStorage diskStorage upload.single uploadImageController cloudinary uploader.upload buffer secure_url uploads destination filename imageUrl",
    "Where do we detect that the browser window is too narrow and switch to the phone layout?": "useMobile isMobile useMediaQuery matchMedia window.innerWidth resize addEventListener breakpoint useEffect useState handleResize responsive",
    "When the API rejects a call as unauthorized, how does the app recover instead of bouncing the user back to the sign-in screen?": "axiosInstance interceptors.response 401 refreshAccessToken refreshToken retry originalRequest Authorization Bearer eject fetchNewAccessToken logout",
    "If a shopper taps Add on something that's already in their basket, what stops a duplicate row being written?": "addToCart cartItem findOne existingItem checkItemCart quantity increment updateQuantity productId userId alreadyInCart upsert",
    "What stops someone from removing a top-level section that still has things filed under it?": "deleteCategory categoryId subCategory countDocuments checkSubCategory checkProduct find length error 400 cannotDelete referenced",
    "After a customer pays through Stripe's hosted checkout, how does the order actually get written to our database?": "webhookStripe constructEvent checkout.session.completed endpointSecret stripe-signature lineItems metadata orderModel insertMany createOrder paymentId sessionId",
    "How does product search actually match text \u2014 is it a regex scan or a real index?": "searchProduct $text $search $regex $options textIndex createIndex name description query keyword aggregate",
    "Where do we gate the staff-only screens so a normal shopper can't reach them?": "AdminPermission admin isAdmin role ADMIN protectedRoute AdminRoute middleware auth Outlet Navigate isAdminMiddleware",
    "Typing quickly into the catalog management filter seems to fire a request per character \u2014 is there any throttling?": "debounce setTimeout clearTimeout useDebounce delay searchText handleOnChange useEffect throttle onChange timer",
    "Our listing pages have URLs like /Dairy-Bread-6734abc/Milk-6712def \u2014 where is that slug assembled and where is the id pulled back out of it?": "validURLConvert valideURLConvert slug slugify replace toLowerCase split pop _id subCategoryId categoryId useParams Link",
    "A user removed a saved delivery location but the record is still sitting in the database \u2014 is that on purpose?": "deleteAddress status isActive false softDelete updateOne findByIdAndUpdate addressModel deleteAddressController flag archived userId"
  },
  "netty": {
    "If the app keeps handing us data faster than the socket drains it, how does Netty tell the app to slow down instead of just growing the queue forever?": "isWritable channelWritabilityChanged WriteBufferWaterMark writeBufferHighWaterMark writeBufferLowWaterMark ChannelOutboundBuffer totalPendingSize setUnwritable incrementPendingOutboundBytes backpressure unwritable",
    "How does Netty warn me at runtime that a buffer got thrown away without being freed, and how do I make it show where it was allocated?": "ResourceLeakDetector ResourceLeakTracker leakDetectionLevel PARANOID ADVANCED SIMPLE AdvancedLeakAwareByteBuf SimpleLeakAwareByteBuf touch record reportTracedLeak refCnt",
    "What does the resolver do when a nameserver's answer is too big to fit in a single UDP datagram?": "isTruncated truncated TC DnsNameResolver DnsQueryContext TcpDnsQueryEncoder TcpDnsResponseDecoder socketChannelFactory retryWithTcp DatagramDnsResponse DnsResponse edns",
    "How do we defend against an HTTP/2 peer that spams us with pings and stream resets but never reads our replies?": "Http2ControlFrameLimitEncoder maxOutstandingControlFrames Http2ConnectionHandler Http2FrameCodec rstFrame ping ack Http2Exception ENHANCE_YOUR_CALM goAway flush limit",
    "How does the server choose which certificate/SSL context to use based on the domain the client is connecting to?": "SniHandler AbstractSniHandler DomainNameMapping DomainWildcardMappingBuilder Mapping lookup hostname serverName SslContext SniCompletionEvent replaceHandler AsyncMapping",
    "How does Netty decide how many bytes to ask for on each socket read, and does it grow or shrink that guess over time?": "RecvByteBufAllocator AdaptiveRecvByteBufAllocator FixedRecvByteBufAllocator guess record allocate Handle SIZE_TABLE INDEX_INCREMENT INDEX_DECREMENT lastBytesRead maxMessagesPerRead",
    "When a new connection is accepted, what picks the worker thread that will own it from then on?": "EventExecutorChooserFactory DefaultEventExecutorChooserFactory PowerOfTwoEventExecutorChooser next MultithreadEventExecutorGroup MultithreadEventLoopGroup register childGroup ServerBootstrapAcceptor EventLoop roundRobin",
    "If nothing in my pipeline consumes an inbound message, does it get freed or does it leak?": "TailContext onUnhandledInboundMessage ReferenceCountUtil release DefaultChannelPipeline SimpleChannelInboundHandler autoRelease refCnt discarded fireChannelRead ByteBuf",
    "With the pooled allocator, how is a thread mapped to one of the memory pools, and what stops all threads from piling onto the same one?": "PooledByteBufAllocator PoolArena PoolThreadCache PoolThreadLocalCache FastThreadLocal leastUsedArena numHeapArenas numDirectArenas initialValue numThreadCaches arenaIndex",
    "My handler still gets flooded with events right after I turn off auto-read, because the decoder emitted several messages from one read. How do I make it hand me one at a time?": "FlowControlHandler setSingleDecode isSingleDecode ByteToMessageDecoder setAutoRead ChannelConfig read dequeue queue RecyclableArrayDeque channelReadComplete ctx",
    "A message arrives split across several TCP segments \u2014 where are the leftover bytes kept between reads, and what keeps that buffer from growing without bound?": "cumulation Cumulator MERGE_CUMULATOR COMPOSITE_CUMULATOR ByteToMessageDecoder discardSomeReadBytes discardAfterReads numReads TooLongFrameException maxFrameLength LengthFieldBasedFrameDecoder callDecode",
    "What kills a TLS connection if the client connects but never finishes the handshake?": "SslHandler handshakeTimeoutMillis setHandshakeTimeout handshakePromise SslHandshakeTimeoutException schedule tryFailure handshakeFuture closeOutboundAndChannel IdleStateHandler notifyHandshakeFailure"
  },
  "spring-framework": {
    "What decides whether a bean ends up behind a JDK dynamic proxy or a CGLIB subclass proxy?": "DefaultAopProxyFactory createAopProxy AopProxy JdkDynamicAopProxy ObjenesisCglibAopProxy proxyTargetClass optimize AdvisedSupport ProxyConfig hasNoUserSuppliedProxyInterfaces ProxyFactory getProxiedInterfaces",
    "Two of our services are wired into each other and the app still boots \u2014 how does the container manage that without looping forever?": "DefaultSingletonBeanRegistry singletonFactories earlySingletonObjects getSingleton addSingletonFactory allowCircularReferences singletonsCurrentlyInCreation BeanCurrentlyInCreationException ObjectFactory getEarlyBeanReference earlyProxyReferences AbstractAutowireCapableBeanFactory",
    "A checked exception escapes our service method but the database writes still get committed \u2014 where is that decided, and how do I change it for one exception type?": "rollbackOn RuleBasedTransactionAttribute RollbackRuleAttribute NoRollbackRuleAttribute DefaultTransactionAttribute rollbackFor noRollbackFor TransactionAspectSupport completeTransactionAfterThrowing RuntimeException TransactionAttribute setRollbackOnly",
    "How long do we wait between attempts after something fails, and is there any randomness so all the callers don't hammer the server at the same instant?": "BackOff BackOffExecution ExponentialBackOff FixedBackOff initialInterval maxInterval multiplier jitter RetryTemplate RetryPolicy maxAttempts nextBackOff",
    "When a client disconnects in the middle of a streaming response, what stops the app from continuing to write into a dead output stream?": "ResponseBodyEmitter SseEmitter StreamingResponseBody DisconnectedClientHelper AsyncRequestNotUsableException ClientAbortException isClientDisconnectedException AsyncListener onError completeWithError WebAsyncManager ServletServerHttpResponse",
    "Why doesn't the whole application get rebuilt for every test class in our suite, and what makes two test classes share the same one?": "ContextCache DefaultContextCache CacheAwareContextLoaderDelegate MergedContextConfiguration hashCode equals TestContextManager DirtiesContext ContextCustomizer contextCache maxSize loadContext cacheKey",
    "If two controller methods both match an incoming URL, how is the winner chosen and when does it blow up as ambiguous?": "AbstractHandlerMethodMapping lookupHandlerMethod addMatchingMappings getMatchingMapping MatchComparator getMappingComparator RequestMappingInfo compareTo IllegalStateException Ambiguous PatternsRequestCondition RequestMappingInfoHandlerMapping",
    "At what point does a SpEL expression stop being interpreted and get turned into bytecode?": "SpelCompiler SpelCompilerMode compiledAst CompiledExpression interpretedCount SpelExpression checkCompile isCompilable generateCode SpelParserConfiguration IMMEDIATE MIXED",
    "What exactly becomes the cache key when a @Cacheable method takes zero, one, or several arguments?": "KeyGenerator SimpleKeyGenerator SimpleKey generateKey EMPTY CacheAspectSupport CacheOperationExpressionEvaluator keyGenerator CacheOperationContext generateKey params SpEL",
    "A background fire-and-forget method blew up and nobody noticed \u2014 where does that exception actually go?": "AsyncUncaughtExceptionHandler SimpleAsyncUncaughtExceptionHandler AsyncExecutionAspectSupport handleError AsyncConfigurer getAsyncUncaughtExceptionHandler AsyncAnnotationAdvisor doSubmit Future ErrorHandler TaskExecutor Async",
    "How does the framework choose between JSON and XML for a response body, and where does the 406 come from?": "ContentNegotiationManager ContentNegotiationStrategy HeaderContentNegotiationStrategy HttpMessageConverter writeWithMessageConverters AbstractMessageConverterMethodProcessor getProducibleMediaTypes HttpMediaTypeNotAcceptableException MediaType canWrite sortBySpecificity produces",
    "How is the next fire time computed from a cron string like '0 0 3 * * MON-FRI'?": "CronExpression CronTrigger CronSequenceGenerator nextExecution nextExecutionTime CronField BitsCronField QuartzCronField parse rollForward Temporal fields"
  },
  "Signal-Android": {
    "When a job fails, how long do we wait before trying again, and what makes us finally give up on it?": "JobManager Job Parameters onShouldRetry runAttempt maxAttempts lifespan backoff BackoffUtil exponentialBackoff nextRunAttemptTime onFailure",
    "I need to add a column to one of the tables \u2014 where does the schema version live and how do I register the upgrade step?": "SignalDatabase SignalDatabaseMigrations DATABASE_VERSION onUpgrade SQLiteOpenHelper migrate Migration ALTER_TABLE DatabaseTable OpenHelper schema",
    "How do we decide whether an outgoing message can be delivered without the server learning who it came from?": "UnidentifiedAccess SealedSender SealedSenderAccess UnidentifiedAccessUtil unidentifiedAccessKey SenderCertificate ProfileKey AccessControl UNRESTRICTED sealedSenderAccessFor UnidentifiedDelivery",
    "Where is the priority order for those one-time promo/nag cards that show at the top of the chat list, and how do I add another one?": "Megaphone Megaphones MegaphoneRepository MegaphoneRecord Event buildDisplayOrder priority Schedule RecurringSchedule MegaphoneViewBuilder seen",
    "For users in countries where the service is blocked, how do we end up talking to different hosts?": "CensorshipCircumvention SignalServiceConfiguration censored countryCode e164 domainFronting proxy HostConfig fallback ServiceConfig ApiConfig NetworkConfiguration",
    "In a very active group chat my phone makes noise for every single message \u2014 where would I change that?": "MentionSetting notifyForMentionsOnly muteUntil NotificationChannels MessageNotifier CustomNotificationsSettings RecipientTable NotificationProfile ALWAYS_NOTIFY DO_NOT_NOTIFY vibrate ringtone",
    "What decides whether a chat from someone I've never talked to shows the accept/block bar instead of the normal compose box?": "MessageRequest MessageRequestState MessageRequestRepository MessageRequestsViewModel isMessageRequestAccepted profileSharing RecipientUtil InputPanel ConversationFragment hasSentMessage blocked ConversationBanner",
    "Once the countdown on a self-destructing message runs out, what actually removes it from the device?": "ExpiringMessageManager expiresIn expireStarted expiration scheduleDeletion checkSchedule deleteMessage MessageTable TrimThreadJob disappearing ExpirationInfo executor",
    "How long do we keep the socket alive after the user leaves the app, and when do we fall back to push instead?": "IncomingMessageObserver SignalWebSocket appVisible keepAlive foregroundService FcmFetchManager websocketDrainTimeout MAX_BACKGROUND_TIME censorship KeepAliveSendJob timeout pushNotification",
    "Why does the app demand my passphrase again after I've had it in the background for a while, and where is that delay configured?": "KeyCachingService passphrase timeout PassphraseRequiredActivity MasterSecret TextSecurePreferences getPassphraseTimeoutInterval AlarmManager clear lock screenLock ScreenLockTimeout",
    "How do I add a value the server can toggle, and why doesn't changing it take effect until the app restarts?": "RemoteConfig FeatureFlags hotSwappable sticky REMOTE_CAPABLE FORCED_VALUES RemoteConfigRefreshJob getBoolean update init memoryValues buckets",
    "I want to run some initialization at launch but not delay the first screen from drawing \u2014 what's the right hook?": "ApplicationContext onCreate AppStartup AppInitialization addBlocking addNonBlocking addPostRender onCriticalRenderEventStart Initializer ApplicationDependencies SignalStore onRenderComplete"
  },
  "OwnCloud": {
    "Why do folders always appear before files in a directory listing, and how does name ordering handle accented characters and capitals?": "FileListOrder SortType SortOrder Comparator Collator sortFolder compareToIgnoreCase isFolder ascending FileStorageUtils sortByName normalize",
    "How long do we keep the app's log files on the device before they get deleted, and what runs that cleanup?": "LogsProvider Logger LogsRepository OldLogsCollectorWorker deleteOldLogs retention LOGS_RETENTION_DAYS WorkManager PeriodicWorkRequest logsDirectory expiration cleanup",
    "When a big file is uploaded, how large are the pieces it's split into, when does the app decide to split at all, and why does the last piece get a much longer timeout?": "ChunkedUploadRemoteFileOperation CHUNK_SIZE chunk threshold isChunkedUpload lastChunk timeout readTimeout assembly transferId uploadChunk PutMethod",
    "If I send up a file and something with that exact name is already sitting on the server, what does the app end up calling it?": "NameCollisionPolicy RENAME getAvailableRemotePath checkNameCollision ExistenceCheckRemoteOperation suffix remotePath UploadFileOperation FORBIDDEN_CHARS overwrite behaviour",
    "What stops the camera-roll backup from sending the same photos over and over on every run?": "CameraUploadsWorker FolderBackUpConfiguration lastSyncTimestamp syncTimestamp MediaStore DATE_ADDED alreadyUploaded checkpoint PictureUploads VideoUploads AutomaticUpload",
    "If someone edits a document on their phone while it also changed on the server, how does the app notice and what does it do about it?": "SynchronizeFileOperation etag etagInConflict SYNC_CONFLICT ConflictsResolveDialog keepBoth modificationTimestamp lastSyncDateForData ConflictResolution overwrite CONFLICT propfind",
    "Why doesn't the whole browser list flash/rebuild every time one item's state changes?": "DiffUtil ItemCallback areItemsTheSame areContentsTheSame ListAdapter notifyItemChanged payload FileListAdapter submitList RecyclerView ViewHolder notifyDataSetChanged",
    "Where do we decide that the user has to enter their passcode again after leaving the app, and where do the delay options come from?": "PassCodeManager PassCodeActivity BiometricManager LockTimeout PREF_LOCK_TIMEOUT SettingsSecurityFragment lastUnlockTimestamp visibleActivities onActivityStopped lockTimeoutValues SecurityEnum",
    "When the server answers 401 on a request, do we transparently get fresh credentials and retry, or does the error surface to the caller?": "OwnCloudClient executeHttpMethod HTTP_UNAUTHORIZED refreshCredentials invalidateAuthToken AccountManager OwnCloudCredentials Interceptor RemoteOperationResult UNAUTHORIZED retry AuthenticatorActivity",
    "If a user opens one of our files in another app through the Android file picker and saves changes there, how do those edits make it back to the server?": "DocumentsStorageProvider openDocument ParcelFileDescriptor OnCloseListener MODE_WRITE DocumentsContract UploadFileFromContentUri uploadUpdatedFile ContentResolver TransferManager observer FileDescriptor",
    "When someone chooses the 'upload only' option on a public link, what number actually gets sent to the server for it?": "RemoteShare permissions CREATE_PERMISSION_FLAG ShareType PUBLIC_LINK UPLOAD_ONLY publicUpload CreateRemoteShareOperation READ_ONLY capabilities publicUploadPermission shareViaLink",
    "I need to add a column to one of the stored entities \u2014 what do I have to update so people upgrading from an older build don't lose data or crash on first launch?": "OwncloudDatabase RoomDatabase Migration DATABASE_VERSION Entity ColumnInfo addMigrations ALTER TABLE ProviderMeta schemas fallbackToDestructiveMigration"
  },
  "DDG": {
    "What decides whether the shield icon next to the URL is shown plain, with a dot, or as an alert?": "PrivacyIcon PrivacyIconView PrivacyIconLogic shield shieldWithDot alert OmniBar privacyInfo protectionStatus isProtected unprotectedTemporary trackersBlocked",
    "Where does the app handle moving between foreground and background, and what happens if an unexpected lifecycle event arrives?": "AppStateMachine AppLifecycle AppState Foreground Background Suspending Launching AppDelegate SceneDelegate handleUnexpectedEvent unexpectedTransition pixel",
    "If a user turns on clearing tabs and data after inactivity, where is the delay actually checked?": "AutoClear AutoClearSettings ClearDataOption shouldClearData timing elapsedTime backgroundDate AppDataClearer AutoClearWorker AutoClearSettingsModel inactivity delay",
    "Why do the top and bottom UI strips slide out of the way when you scroll a web page, and what makes them come back?": "BrowserChromeManager ChromeDelegate BrowserChromeDelegate setBarsHidden hideBars showBars scrollViewDidScroll omniBar toolbar contentOffset transition animate",
    "How long can someone keep viewing their saved logins before we make them prove who they are again?": "AutofillLoginListAuthenticator DeviceAuthenticator authenticationTimeout authenticate LAContext biometric authenticationState invalidate secureVault autofill expiry timeout",
    "When browsing data gets wiped, which sites keep their cookies and local storage, and where is that exception list kept?": "PreserveLogins Fireproofing FireproofDomains allowedDomains fireproofed WebCacheManager clearData cookieStorage websiteDataStore removeData UserDefaults exceptions",
    "When you start typing in the search field, how are your saved bookmarks ordered so the most likely one shows first?": "BookmarksCachingSearch BookmarksStringSearch ScoredBookmark score relevance sortedByScore AutocompleteSuggestion tokens query prefix title domain",
    "If someone clicks through the big red phishing warning and loads the site anyway, how do we remember not to block them again?": "MaliciousSiteProtection SpecialErrorPage phishing malware ThreatKind bypass bypassedURLs visitSite exemption navigationHandler leaveSite allowlist",
    "A tab's web view gets thrown away under memory pressure \u2014 how do we bring back its scroll position and half-filled forms when the user returns to it?": "interactionState restoreInteractionState TabInteractionStateSource sessionStateData TabViewController webViewWebContentProcessDidTerminate memoryPressure attachWebView restore scrollPosition formData snapshot",
    "If a usage-stat request fails because the phone was offline, do we ever try again, and how long do we hold onto it?": "Pixel PixelFiring DailyPixel retry retries FailedPixelStore pixelStore timestamp expiry enqueue URLSession offline",
    "How can we tell that someone deleted the app and installed it again rather than being a genuinely first-time user?": "ReturnUserMeasurement isReturningUser reinstall keychain atb installDate firstLaunch StatisticsStore StatisticsLoader variant newUser AppInstallStore",
    "The server rejects a sync upload with a 413 or a 429 \u2014 where do we turn that into the message the user sees, and how do we stop nagging them repeatedly?": "SyncErrorHandler SyncError SyncErrorMessage payloadTooLarge tooManyRequests statusCode unexpectedStatusCode shouldShowAlert lastErrorNotificationTime dismissed SyncBookmarksAdapter alert"
  }
}